"""
llmscrapy — An LLM-powered web scraping tool.

Reads URLs from JSON files, fetches webpages, parses HTML,
and uses DeepSeek V4 to extract structured metadata
(publish time, author, reposts, likes, comments, etc.).
"""

__version__ = "0.1.0"
