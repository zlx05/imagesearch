# ── Web UI ──
nicegui>=1.4

# ── HTTP & 网络 ──
requests>=2.31
urllib3>=2.0

# ── 图像处理 ──
Pillow>=10.0
ImageHash>=4.3.2
numpy>=1.24,<3

# ── 配置 & 模型 ──
python-dotenv>=1.0
typing-extensions>=4.8

# ── LLM 调用 ──
openai>=1.0
pydantic>=2.0

# ── HTML 解析（llmscrapy / Retriever）──
beautifulsoup4>=4.12
lxml>=4.9

# ── 以图搜图引擎 ──
PicImageSearch>=4.0

# ── SerpApi（Google Lens）──
serpapi>=0.1

# ── Firecrawl SDK（可选，旧版爬虫路径）──
firecrawl-py>=1.0

# ── mitmproxy（代理抓包）──
mitmproxy>=10.0
pure-python-adb>=0.3

# ══════════════════════════════════════════════════════════════════
# 以下为可选依赖，按需安装
# ══════════════════════════════════════════════════════════════════

# ── CLIP 视觉语义（VALIDATOR_ENABLE_CLIP=true 时需要）──
# torch>=2.0
# torchvision>=0.15
# transformers>=4.30

# ── OCR 引擎（VALIDATOR_ENABLE_OCR=true 时需要）──
# rapidocr-onnxruntime>=1.3
# onnxruntime  (CPU: onnxruntime, GPU: onnxruntime-gpu)
# paddleocr>=2.7
# paddlepaddle  (CPU: paddlepaddle, GPU: paddlepaddle-gpu)

# ── Playwright（llmscrapy DOM 富化 enrich_mode=playwright|both 时需要）──
# playwright>=1.40

# ── LangGraph CLI 测试（workflow.py 独立运行用，主应用不需要）──
# langgraph>=0.2
# langchain-core>=0.3

# ── 开发 / 测试 ──
pytest>=7.0
pytest-mock>=3.10
